package mono.factories.mod.newloader.dml;

import mono.factories.mod.newloader.source.provider.CodeSource;

public class DMLoader implements ModLoader {
    @Override
    public void load(CodeSource source, DMLConfig config) {
        
    }
}
